# Changelog

All notable changes to this project will be documented in this file.

The format is based on `Keep a Changelog`, and this project aims to follow `Semantic Versioning` once public releases begin.

## [Unreleased]

### Added

- Installable local package `autoresearch-agent`
- `research.yaml`-driven project scaffold and runtime flow
- Built-in `prediction_market` pack
- Minimal CLI and stdio `MCP` facade
- Package validation and `PyPI` / `TestPyPI` release workflows
- Open-source support documents for contribution, security, and community expectations

## [0.1.0] - Pending first public release

### Planned

- First public `PyPI` release of `autoresearch-agent`
- Trusted publishing setup for `TestPyPI` and `PyPI`
