# Prediction Market Example

This folder shows the expected local project shape for the first `autoresearch` pack.

The repository version uses JSON-compatible YAML files for the first release, so
the example config can be read without extra dependencies.
